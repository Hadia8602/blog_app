#  DevBlog

A full-stack blog app built with the MERN stack (MongoDB, Express, React, Node.js).

##  Quick Start

### 1. Backend

```bash
cd server
npm install
npm run seed    # Run once to add sample posts
npm start       # http://localhost:3000
### 2. Frontend
cd client       
npm install
npm run dev     # http://localhost:5173